# Agent Proto 修复状态验证 + 遗留问题清单 (v1)

## 📋 背景
之前声称完成了 8 项修复，经代码扫描验证：7 项真实完成，1 项（甘特图）未完成。
以下是遗留问题和新增发现。请严格按此清单执行。

---

## 🐛 遗留问题：甘特图未实现（高优先级）

### 问题描述
`widgets/gantt_chart.py` 当前仅有 19 行，是一个空壳 placeholder：
```python
placeholder = QLabel("📅 甘特图 · 周视图\n\n（Phase 4 填充）")
```
任务助理 Tab 中的甘特图完全没有被构建。

### 修改要求
1. 参考 `DESIGN.md` 和 `BUILD_PLAN.md` 中对甘特图的描述
2. 使用 QPainter 手绘实现（不要引入 matplotlib 等重型依赖）
3. 需要支持：
   - 周视图（7 天横向排列）
   - 时间轴从 0:00 到 24:00，刻度均匀
   - 任务条根据 `schedule_results` 表数据绘制
   - 颜色区分优先级（高=红，中=橙，低=蓝）
   - 手动排程 vs 自动排程的视觉区分
4. 数据源：`database.py` 中的 `get_schedule(date_str)` 方法

---

## 🛠 中优先级：体验优化

### 1. ChatWindow 发送后缺少 loading 反馈
**文件**：`agent_ui.py` `_on_send` 方法（第 418-448 行）
**问题**：用户发送消息后，`do_chat` 通过 `QTimer.singleShot(100, ...)` 异步执行，但 `_add_typing_indicator()` 的显示时机可能不够明显。
**要求**：确保发送后立即显示"小鱼正在打字..."指示器，回复到达后移除。

### 2. IDE 自动休眠逻辑调整
**文件**：`agent_loop.py` 第 573-588 行
**问题**：连续使用 IDE 5 分钟后自动进入休眠态，对正在编码的用户可能造成困惑。
**要求**：
- 将 IDE 休眠阈值从 5 分钟调整为 15 分钟
- 休眠前通过悬浮窗给出 10 秒预告（"检测到编码中，即将进入静默模式..."）
- 或者改为：IDE 模式下仅降低呼吸频率，不进入完全休眠

---

## 🧹 低优先级：代码清理

### 1. 调试 print 清理
**文件**：`agent_loop.py` 第 485、488 行
**问题**：启动时的调试信息会出现在 TerminalPanel 中。
**要求**：这些启动信息可以保留，但建议统一格式，加上 `[启动]` 前缀保持一致性（第 488 行已有，第 485 行没有）。

---

## 🚀 执行指令

1. **先完成甘特图**（最高优先级，这是唯一未完成的修复项）
2. **再处理 ChatWindow loading 反馈和 IDE 休眠调整**
3. **最后清理调试 print**
4. 每完成一项，请简要说明修改了哪些文件、哪些行
5. 所有修改不得破坏现有架构（Observer→Thinker→Actor）和隐私守则
