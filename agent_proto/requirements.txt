# 认知增强系统 v3 — 依赖列表

## 核心依赖（必需）
PyQt5>=5.15.0
pywin32>=306
psutil>=5.9.0
openai>=1.0.0
pyyaml>=6.0
qtawesome>=1.3.0

## 可选依赖
pyperclip>=1.8.0          # 剪贴板监听（无此库则跳过）
plyer>=2.1.0              # Windows 桌面通知（无此库则跳过）
winrt-Windows.Foundation   # 系统通知监听（Windows 10/11 原生 API）
winrt-Windows.UI.Notifications
winrt-Windows.ApplicationModel

## 开发工具（可选）
PyInstaller>=6.0          # 打包为 exe
