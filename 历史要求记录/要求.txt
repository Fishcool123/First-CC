请作为资深Python开发工程师，为我编写一个用于解析和提取Qwen Studio聊天记录JSON文件的自动化脚本。输入文件路径为：`C:\Users\www15\Desktop\First CC\chat-export-1778291923790.json`（约62MB，302个对话）。JSON结构如下：顶层包含`data`数组，每个对话对象含`id`、`title`、`created_at`、`updated_at`及`chat.history.messages`字典。其中用户消息直接存在于`message.content`；由于导出时开启了Thinking模式，AI回复内容实际存储在`message.content_list[].extra.summary_thought.content[]`数组中，思考标题在`summary_title.content[]`。核心需求：1. 解析并提取所有用户提问（过滤<10字符）、AI思考过程、对话元数据（标题/ID/时间/模型）；2. 按对话生成独立Markdown文件（文件名格式`{序号:03d}_{清理后标题}.md`，内容含对话流水、元数据）；3. 生成`index.md`（可点击的对话列表索引）和`statistics.md`（模型使用分布、时间跨度、高频对话主题统计）；4. 实现自动分类（技术配置/学习相关/设备硬件/健康生活/其他）与基于标题文本相似度>0.8的重复检测标记；5. 提供CLI参数支持（`--input`, `--output`, `--category`, `--extract-code`, `--extract-decisions`, `-v`等）。技术要求：使用Python 3.8+，优先标准库（json, os, pathlib, datetime, re），可选`tqdm`进度条；严格处理`NoneType`异常与字段缺失（使用安全取值函数）；时间戳转为`YYYY-MM-DD HH:MM:SS`；中文文件名自动清理`\/:*?"<>|`等非法字符；内存占用控制在512MB内；全量处理时间<30秒。注意事项：导出的AI内容为思考过程非最终回复，需在生成文件中注明；确保全程UTF-8编码；提供隐私过滤选项（如隐藏API密钥/敏感词）；代码需包含完整类型提示与详细注释。最终交付：完整可运行Python脚本、清晰的项目结构、示例输出说明。请严格遵循上述规范，直接输出完整代码及必要配置。